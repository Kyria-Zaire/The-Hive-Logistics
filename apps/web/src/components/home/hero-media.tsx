/**
 * Contrat média Hero : full-bleed, recadrages responsives, priorité LCP.
 * PROD : remplacer par assets validés CNT-004 dans /public.
 */
export function HeroMedia() {
  return (
    <div className="absolute inset-0 thl-media-fallback" aria-hidden="true" />
  );
}
