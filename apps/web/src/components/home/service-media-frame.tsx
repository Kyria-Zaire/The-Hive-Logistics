/** Cadre média service — fallback graphique sans copy (RECETTE/DEV). */
export function ServiceMediaFrame() {
  return (
    <div
      className="aspect-video w-full border border-thl-border bg-[linear-gradient(145deg,#141414_0%,#0a0a0a_100%)] lg:aspect-[4/3]"
      aria-hidden="true"
    />
  );
}
